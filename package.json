{
  "name": "site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "git add -A && git commit -m \"sync: $(date '+%b %d, %Y, %I:%M %p')\" && git push"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
